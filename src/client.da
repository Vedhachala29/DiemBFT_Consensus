import sys
# config(channel is fifo, clock is lamport)

class Client(process):

    def setup(validators: list):  # have to create message class
        self.exit_flag = 0
        pass

    def run():
        # output('Logical time', logical_clock())
        send(('Client', 'Transactions', logical_clock()), to=self.validators)
        await(exit_flag == 1)
