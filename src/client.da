import sys


class Client(process):

    def setup(validators: list):  # have to create message class
        self.exit_flag = 0
        pass

    def run():
        send(('Client', "transactions"), to=self.validators)
        await(exit_flag == 1)
